\section{Ritmo Visual \label{ritmo_visual}}

Ritmo Visual é definido como a representação de um segmento de
imagens $2D+t$ (um vídeo) em uma única imagem na dimensão $2D$,
sendo portanto uma tentativa de tornar o trabalho de análise de
vídeo mais simplificado.

Existem dois tipos de representações do ritmo visual. Ritmo Visual
por histograma e por sub-amostragem.

\subsection{Ritmo Visual por sub-amostragem \label{ritmo_visual_amostragem}}

O Ritmo Visual port sub-amostragem caracteriza-se pela
transformação de determinadas faixas de pixels (amostras) de cada um
dos frames de uma tomada do vídeo em uma coluna da representação $2D$,
como representado na Figura \ref{img_rvdiagonal}. Podemos dizer então que o Ritmo
Visual é uma simplificação do vídeo.

O padrão desta representação pode ser alterado conforme a escolha
dos pixels, portanto, o padrão do ritmo visual é totalmente
dependente do tipo da amostragem retirada do vídeo. Bons resultados
podem ser obtidos se a sub-amostragem do frame for dada pela sua
diagonal principal, uma vez que ela trará características
horizontais e verticais do frame, podendo assim fazer uma boa
representação do mesmo. Uma melhor abordagem sobre este processo
pode ser encontrada em \cite{GUIMARAES_2003_2} e \cite{GUIMARAES_2001_2}.

\begin{figure}[h|top]
 \centering
 \includegraphics[width=0.3\linewidth]{imagens/rv_diagonal.png}
 \caption{Transformação da diagonal principal dos frames de um
vídeo em colunas do Ritmo Visual.}
 \label{img_rvdiagonal}
\end{figure}

Na Figura \ref{img_rvdiagonal} pode ser observado o processo de
transformação das diagonais principais dos frames $1$, $2$, $3$, $4$
e $5$ nas colunas $1$, $2$, $3$, $4$, e $5$ do Ritmo Visual.

Seja $\bigvee = {I_1, I_2, ..., I_n}$ um vídeo e $I_i(x,y)$ um
frame $\in Vi$, com altura $x$ e largura $y$.

Um ponto qualquer de um frame é definido como $p(x,y)$ ou $x,y \in
N$, sendo que $p(x,y) \in [0...L]$, onde $Ii$ é o valor da
luminescência máxima. Com isso, se $p(x,y) = 0$ o ponto torna-se
preto, e se $p(x,y) = L$, o ponto torna-se branco.

Neste trabalho, será adotado $L = 255$.

Portanto, para Ritmo Visual, define-se que:

Seja $D_i$ uma diagonal do frame $I_i$, a sequência de todas as
diagonais $D_i \in \bigvee$, chama-se Ritmo Visual de $\bigvee$.

Cada tipo de transição gera uma coluna com características, padrões
diferentes no ritmo visual, como podem ser observados na Figura:
\ref{img_rv_exemplos}:

\begin{figure}[h|top]
 \centering
 \includegraphics[width=1.0\linewidth]{imagens/rv_exemplos.png}
 \caption{Representações de Ritmo Visual: a) cortes; b) fade-out seguido de fade-in; c) duas transições do tipo dissolve consecutivas}
 \label{img_rv_exemplos}
\end{figure}

Como visto na Figura \ref{img_rv_exemplos}- a, os cortes são
caracterizados no Ritmo Visual por linhas verticais. Porém, pela
possível existência de ruídos na imagem gerada, nem todas as linhas
verticais podem ser necessariamente um corte. Para resolver este
problema, algumas técnicas de filtragem espacial podem ser
aplicadas, diminuindo a intensidade dos ruídos.

\subsection{Ritmo Visual por Histograma\label{ritmo_visual_histograma}}

Como o Ritmo Visual port sub-amostragem, o Ritmo Visual por Histograma ($RVH$)
tem como objetivo simplificar a representação de um vídeo.
O processo de criação de um $RVH$ é :

\begin{itemize}
\item Captura da diagonal principal do frame.
\item Calculo do histograma do frame.
\item Calculo do pico máximo do frame.
\item O ponto do pico máximo respresenta um ponto em nossa reta.
\end{itemize}

Matematicamente:


Seja $Hi$ o histograma do frame $Ii$, e a função $MAX(Hi)$ o valor de luminescência com maior
presença no frame (i.e. o pico máxmimo do histograma).

A sequência de todos os $MAX(Hi) \in \bigvee$ agrupados em uma reta, é chamado de $RVH$

Neste trabalho, as técnicas de Ritmo Visual serão utilizadas para
transformar o nosso problema de segmentação de vídeos em um problema
de segmentação de imagens. A partir daí, faremos uso de outras
ferramentas de PDI como filtragem espacial e detecção de bordas,
juntamente com a aplicação de operadores de morfologia matemática,
sobre o $RV$ com o objetivo de obter os pontos de transição das
tomadas no vídeo $\bigvee$
No $RVH$ serão aplicadas derivadas para uma análise posterior, afim
de encontrarmos alguns tipos de transições (fade,dissolve).
